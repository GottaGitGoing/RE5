#include <functional>

/*
void LinkedList::transformAll(std::function<int(LinkedList*,const int&)> f)
{
    Node* ll = head;
    while (ll != nullptr)
    {
        ll.value = f(ll.value);
        ll = ll.next
    }
}
*/
